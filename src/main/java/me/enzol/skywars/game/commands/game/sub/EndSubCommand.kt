package me.enzol.skywars.game.commands.game.sub

class EndSubCommand {
/*
    @CommandExecutor
    fun execute(player: Player) {
        val skywarsPlayer = SkywarsPlayer.get(player)
        val game = skywarsPlayer.game

        if(game == null){
            player.sendMessage(CC.translate("&cGame not found"))
            return
        }

        game.end()
    }

    override fun getSubCommands(): MutableSet<BaseCommand> = Sets.newHashSet(SoloSubCommand());*/
}