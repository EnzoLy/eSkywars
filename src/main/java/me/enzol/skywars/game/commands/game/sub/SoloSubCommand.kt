package me.enzol.skywars.game.commands.game.sub

class SoloSubCommand{
/*
    @CommandExecutor
    fun execute(player: Player) {
        val map = Map.random()

        if(map == null){
            player.sendMessage(CC.translate("Maps not found"))
            return
        }

        map.verify()

        val game = SoloGame(map)

        Bukkit.getOnlinePlayers().forEach{other ->
            run {
                if(game.players.size < game.maxPlayers){
                    val skywarsPlayer = SkywarsPlayer.get(other);
                    game.players.add(skywarsPlayer)
                    skywarsPlayer.game = game
                }
            }
        }
        game.preStart()
    }*/

}